apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kube-rclone.fullname" . }}-configmap
  labels:
{{ include "kube-rclone.labels" . | indent 4 }}
data:
{{ (.Files.Glob ( print "files/" .Values.rclone.confName)).AsConfig | indent 2 }}
